using Microsoft.VisualBasic;
using MongoDB.Bson.Serialization.Attributes;
using OntimeBackEnd.Controllers;
using OntimeBackEnd.Models;
using OntimeBackEnd.Service;
using System.Collections.Generic;
using Xunit;

namespace OntimeBackEnd.Test
{
    public class OnTimeTest
    {
        
        //[Fact]
        //public async void TestOnTime()
        //{
        //    var onTimeController = new OnTimeController( new OntimeClass()) ;
        //    var res =await onTimeController.InsertRecord(request);
        //}
        public void OperationalFuction()
        {
            var cityDictionary = new Dictionary<string, List<string>>();
            foreach(var city in cityDictionary)
            {
                
            }
        }
    }
}
